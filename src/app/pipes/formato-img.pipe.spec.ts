import { FormatoImgPipe } from './formato-img.pipe';

describe('FormatoImgPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoImgPipe();
    expect(pipe).toBeTruthy();
  });
});
